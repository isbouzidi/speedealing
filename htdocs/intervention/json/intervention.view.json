{
   "_id": "_design/Intervention",
   "_rev": "2-169ba45be03fe7faef1e539e1bca33ba",
   "language": "javascript",
   "views": {
       "list": {
           "map": "function(doc) {\nif (doc.class && doc.class == \"Intervention\" && doc.trash != true && (doc.Status != \"CANCELED\" && doc.Status != \"CLOSED\" && doc.Status != \"DRAFT\")){\n  emit(doc._id, doc);\n }\n}"
       }
   },
   "updates": {
       "in-place": "function (doc, req) {\n var field = req.query.field;\n var value = req.query.value;\n doc[field] = value;\n  return [doc, value];\n }"
   }
}